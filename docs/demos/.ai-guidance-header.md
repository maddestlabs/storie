<!-- 
  STORIE CODE STYLE: For AI Assistants & Developers
  
  Use the TWO-BLOCK PATTERN:
  
  1. Raw `js` blocks for persistent state:
     ✅ let playerX = 10;
     ✅ let score = 0;
  
  2. Lifecycle blocks (on:*) use them naturally:
     ✅ playerX++;  (persistent var - automatically accessible)
     ✅ const dx = 5;  (local var - normal JavaScript)
  
  Avoid verbose scope patterns:
  ❌ scope.state = scope.state || { playerX: 10 };
  ❌ scope.state.playerX++;
  
  See: docs/CODE_STYLE_GUIDE.md
-->
